<div class="container">
  <div class="row">
    <h3>Users</h3>
    <div class="column">
      <table class="table">
        <tbody>
          <% @users.each do |user| %>
            <tr>
              <%= render user %>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
  <% if current_user.try(:admin?) %>
    <%= link_to "Novo Usuário", new_admin_user_path, class: "btn btn-success" %>
  <% end %>
</div>
